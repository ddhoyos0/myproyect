  <p id="notice"><%= notice %></p>

  <h1 align="center">Marcas</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Nombre</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @marcas.each do |marca| %>
        <tr>
          <td><%= marca.nombre %></td>
          <td><%= link_to 'Mostrar', marca,  class:"btn btn-info btn-sm"%></td>
          <td><%= link_to 'Editar', edit_marca_path(marca), class:"btn btn-warning btn-sm"%></td>
          <td><%= link_to 'Eliminar', marca, method: :delete, data: { confirm: 'Esta seguro?' } ,class:"btn btn-danger btn-sm" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <br>
  <%= link_to 'Añadir Marca', new_marca_path, class:"btn btn-success"%>
